<button type="button"
        class="carousel__pagination-btn"
        data-carousel-target="pagination"
        data-action="carousel#goToSlide"
        data-index="<%= index %>"
        aria-label="Go to slide <%= index + 1 %>">
  <span class="carousel__pagination-dot"></span>
