<div style="display: grid; grid-template-columns: repeat(4, 1fr); gap: var(--size-8); padding: var(--size-6); max-width: 1200px;">
  <% components.each do |component| %>
    <%= render component %>
  <% end %>
</div>
